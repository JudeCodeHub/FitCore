# memberships module

The member-facing side of memberships — "what's my own membership."
Admin membership management (creating, freezing, changing plans,
family members) doesn't have a UI yet; only the backend exists so far.

**Data deps:** `apps/api` — `GET /memberships/me`.
