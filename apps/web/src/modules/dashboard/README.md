# dashboard module

Empty per-role home pages — placeholders until each role's real
features are built out phase by phase.

**Routes:** `/admin`, `/trainer`, `/front-desk`, `/member`
(auth + role guard lives in each route's `layout.tsx`, not here).
